% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_svg.R
\name{ggplot2_svg}
\alias{ggplot2_svg}
\title{Save a ggplot2-Object as SVG-File}
\usage{
ggplot2_svg(path = NULL, plot = ggplot2::last_plot(), width = 100, height = 64)
}
\arguments{
\item{path}{Character string giving the path where the file should be saved.
If NULL, saves to "results/plot.svg" in the working directory.}

\item{plot}{Plot object to save. Defaults to last plot displayed.}

\item{width}{Width of the plot in mm. Default is 100.}

\item{height}{Height of the plot in mm. Default is 64.}
}
\value{
A file in svg-format is exported to the results folder in the work directory.
}
\description{
A wrapper around ggsave to export plots in SVG format with dimensions
specified in millimeters, optimized for scientific publications.
It automatically creates the results-directory if it does not exist.
}
\examples{
library(ggplot2)

# 1. Create a test plot
p <- ggplot(mtcars, aes(mpg, wt)) + geom_point()

# 2. Save to a temporary file
temp_path <- file.path(tempdir(), "test_plot.svg")
ggplot2_svg(path = temp_path, plot = p)

# 3. Show the file exists and open it to see the result
if (file.exists(temp_path)) {
  message("SVG successfully saved to: ", temp_path)
  # This will open the SVG in your default browser or viewer:
  utils::browseURL(temp_path)
}
}
